with
    union_ventas as (select * from {{ ref('base_join_ventas') }}),

    fct_ventas as (
        select
        id_cliente,
        id_articulo,
        id_color,
        id_marca,
        id_familia, 
        id_seccion,
        id_genero,
        id_descripcion,
        cantidad,
        venta,
        id_fecha


         from union_ventas
    )

    select * from fct_ventas